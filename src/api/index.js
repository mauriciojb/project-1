const Usuario = document.getElementById('.usuario');
const senha = document.getElementById('.senha');
const btn = document.getElementById('.button');
btn.addEventListener('click', function () {
    
    function CriaUser(user,pass) {
        const login = {
            Usuario: Usuario,
            Senha:senha,
        }
    }

    
} )
